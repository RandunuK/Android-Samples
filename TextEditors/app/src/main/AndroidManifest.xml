<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fusionlab.texteditors">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextEditors">
        <activity
            android:name=".VHScrollWithFixedLineNumbersActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".VHScrollWithLineNumbersActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".VScrollWithLineNumbersActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".VHScrollActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".VScrollActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>